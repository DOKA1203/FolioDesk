<Window x:Class="FolioDesk.FolioFolderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="FolioDesk" Width="96" SizeToContent="Height" MinHeight="116"
        WindowStyle="None" ResizeMode="NoResize" AllowsTransparency="True" Background="Transparent"
        ShowInTaskbar="False"
        Opacity="0.97">
    <Window.Resources>
        <Style TargetType="{x:Type Window}">
            <Setter Property="WindowChrome.WindowChrome">
                <Setter.Value>
                    <WindowChrome CaptionHeight="0"
                                  CornerRadius="2"
                                  GlassFrameThickness="0"
                                  NonClientFrameEdges="None"
                                  ResizeBorderThickness="3"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="20" BorderThickness="0" Padding="8,4,8,8"
            Background="#CC2D2D2D" AllowDrop="True">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="20"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 설정 버튼 -->
            <Button x:Name="SettingsButton" Grid.Row="0"
                    HorizontalAlignment="Right" VerticalAlignment="Center"
                    Width="20" Height="20"
                    FontSize="12"
                    Cursor="Hand"
                    Click="SettingsButton_Click"
                    ToolTip="아이콘 색상 설정">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Foreground" Value="#99FFFFFF"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="Bg" CornerRadius="4"
                                            Background="{TemplateBinding Background}">
                                        <ContentPresenter HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bg" Property="Background" Value="#33FFFFFF"/>
                                            <Setter Property="Foreground" Value="White"/>
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter TargetName="Bg" Property="Background" Value="#55FFFFFF"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
                ⚙
            </Button>

            <ItemsControl x:Name="AppFolderPanel" Grid.Row="1" HorizontalAlignment="Left">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel HorizontalAlignment="Center"
                                   VerticalAlignment="Top"
                                   ItemWidth="80" ItemHeight="100"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="6" CornerRadius="12"
                                AllowDrop="True"
                                Cursor="Hand" Focusable="True"
                                PreviewMouseLeftButtonDown="Icon_PreviewMouseDown"
                                MouseMove="Icon_MouseMove"
                                MouseLeftButtonUp="Icon_Click"
                                DragOver="Icon_DragOver"
                                Drop="Icon_Drop">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#33FFFFFF"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel Orientation="Vertical" HorizontalAlignment="Center"
                                        VerticalAlignment="Center">
                                <Image Source="{Binding Icon}" Width="48" Height="48" Margin="0,4,0,4"
                                       RenderOptions.BitmapScalingMode="HighQuality"/>
                                <TextBlock Text="{Binding Name}" FontSize="11"
                                           HorizontalAlignment="Center"
                                           Foreground="White"
                                           TextTrimming="CharacterEllipsis"
                                           MaxWidth="72"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <Canvas x:Name="DragLayer" IsHitTestVisible="False" Grid.RowSpan="2"/>
        </Grid>
    </Border>
</Window>
